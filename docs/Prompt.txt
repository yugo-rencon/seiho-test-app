以下のルールで、指定科目の各ファイルについて relatedProblems を埋めてください。

【対象】
- 科目: 〇〇
- 対象ファイル: resources/js/Pages/Daigaku/Tests/〇〇/〇〇*.vue

【やってほしいこと】
1. 年度ごとの類似問題をまとめる
   - 例: 2025年は 2025a / 2025b / 2025c の中だけで紐づける
   - 別年度の問題とは紐づけない
2. questionTitle が同一のもの、または明らかに同一テーマと判断できるものを同じグループにする
3. 判断に迷うタイトル差は勝手に紐づけず、実行前に確認する
4. relatedProblems には必ず自分自身も含める
   - 類似問題がない場合も空配列にせず、自分自身のみを入れる
   - 例: ['2025a11']
5. 類似問題がある場合は、自分自身を含めて同一年内の関連問題を入れる
   - 例: ['2025a1', '2025b1', '2025c1']
6. QuestionTemp1 / QuestionTemp3 は :relatedProblems="[...]"
7. QuestionTemp2 の items は relatedProblems: [...]
8. QuestionTemp3 は 5問1ブロックとして扱い、questionRange の先頭番号を問題番号として使う
   - 例: 25〜29 は 2025a25
9. 更新後、空の relatedProblems が残っていないか確認する
10. 最後に npm run build で確認する

【補足】
- 表記ゆれのうち、明らかな補足表記は同一扱いしてよい
  - 例: 住宅借入金等特別控除 と 住宅借入金等特別控除（住宅ローン控除）
- ただし、意味が変わる可能性があるものは確認する

============================================================================
以下のルールで、指定科目の各ファイルについて relatedProblems を埋めてください。

【対象】
- 科目: 〇〇
- 対象ファイル: resources/js/Pages/Tests/〇〇/〇〇*.vue

【やってほしいこと】
1. 年度ごとの類似問題をまとめる
   - 例: 2025年は 2025a / 2025b / 2025c の中だけで紐づける
   - 別年度の問題とは紐づけない
2. questionTitle が同一のもの、または明らかに同一テーマと判断できるものを同じグループにする
3. 判断に迷うタイトル差は勝手に紐づけず、実行前に確認する
4. relatedProblems には必ず自分自身も含める
   - 類似問題がない場合も空配列にせず、自分自身のみを入れる
   - 例: ['2025a11']
5. 類似問題がある場合は、自分自身を含めて同一年内の関連問題を入れる
   - 例: ['2025a1', '2025b1', '2025c1']
6. QuestionTemp1 / QuestionTemp3 は :relatedProblems="[...]"
7. QuestionTemp2 の items は relatedProblems: [...]
8. QuestionTemp3 は 5問1ブロックとして扱い、questionRange の先頭番号を問題番号として使う
   - 例: 25〜29 は 2025a25
9. 更新後、空の relatedProblems が残っていないか確認する
10. 最後に npm run build で確認する

【補足】
- 表記ゆれのうち、明らかな補足表記は同一扱いしてよい
  - 例: 住宅借入金等特別控除 と 住宅借入金等特別控除（住宅ローン控除）
- ただし、意味が変わる可能性があるものは確認する

============================================================================

Keiri202Xx を、添付の解答表で確認して。

対象:
- resources/js/Pages/Tests/Keiri/Keiri202Xx.vue
- docs/memo/生命保険講座/Keiri-progress.md

確認ルール:
1. 問1〜30
   - labels が解答表どおりか確認する
   - 合っていても rev は入れない

2. 問31〜40
   - 解答表で「正」の問題だけ確認する
   - 対応する content が「正しい」なら、Keiri2025a と同じ位置に `// rev: YYYY-MM-DD 確認済み` を入れる
   - 解答表で「誤」の問題は触らない

3. 問41〜50
   - label が解答表どおりか確認する
   - 合っていても rev は入れない

4. 進捗表
   - 追加した rev 件数を Keiri-progress.md に反映する
   - 更新日時、全体の完了数/進捗率、対象ファイルの完了数/進捗率、未完了一覧を更新する

注意:
- rev の位置は Keiri2025a の書き方に合わせる
- labels や content が解答表と違う場合は修正する
- 余計な整形や unrelated な変更はしない

============================================================================

Keiri202Xx の指定範囲を、添付の選択肢画像と照合して確認して
確認できたものだけ rev を入れ、進捗表も更新して。

対象:
- resources/js/Pages/Tests/Keiri/Keiri202Xx.vue
- docs/memo/生命保険講座/Keiri-progress.md

確認範囲:
- 問1〜20
- 問21〜24
- 問41〜50

作業ルール:
1. 添付画像の選択肢と、Vue ファイル内の answer / labels / label / content が一致しているか確認する
2. 一致している問題には `// rev: YYYY-MM-DD 確認済み` を追加する
3. rev の位置は Keiri2025a の書き方に合わせる
   - QuestionTemp4: 各 item の `answer` の直前
   - QuestionTemp3: `contents` 配列の先頭
   - QuestionTemp2 items: 各 item の `questionTitle` の直前
4. すでに rev がある問題には重複して追加しない
5. 指定範囲外は確認・修正しない
6. 不一致があれば、正しい値に修正してから rev を入れる
7. 判断できないものは勝手に rev を入れず、どこが不明か報告する

進捗表:
- 追加した rev 件数を Keiri-progress.md に反映する
- 全体の完了数/進捗率を更新する
- 対象ファイルの完了数/進捗率を更新する
- 未完了一覧から、今回 rev を入れた問題番号を除外する

注意:
- 余計な整形や unrelated な変更はしない
- 既存の未コミット変更は戻さない
- 最後に、追加した rev 件数、更新後の進捗、未完了一覧を簡潔に報告する
